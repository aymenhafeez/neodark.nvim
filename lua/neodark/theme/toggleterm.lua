local toggleterm = {}

function toggleterm.setup(palette)
  local toggleterm_ = {
    -- ToggleTerm1Normal = { bg = palette.bg_d },
    -- ToggleTerm1NormalFloat = { bg = palette.bg_d },
  }

  return toggleterm_
end

return toggleterm
